{
  "name": "hardhat-project",
  "scripts": {
    "test": "cross-env NODE_OPTIONS=--openssl-legacy-provider npx hardhat test",
    "deploy": "cross-env NODE_OPTIONS=--openssl-legacy-provider npx hardhat run scripts/deploy.js --network rinkeby",
    "run": "cross-env NODE_OPTIONS=--openssl-legacy-provider npx hardhat run scripts/sample-script.js",
    "commit": "git add . && git commit -m ' "
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "chai": "^4.3.4",
    "cross-env": "^7.0.3",
    "ethereum-waffle": "^3.4.0",
    "ethers": "^5.5.1",
    "hardhat": "^2.6.8"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.3.3"
  }
}
